# 算法模板总结

> 作者：Sakurakouji Runa  
> 涵盖常用算法与数据结构模板，通俗易懂，可直接使用。

---

## 目录

1. [二分查找](#1-二分查找)
2. [二分答案](#2-二分答案)
3. [DFS（深度优先搜索）](#3-dfs深度优先搜索)
4. [BFS（广度优先搜索）](#4-bfs广度优先搜索)
5. [离散化](#5-离散化)
6. [树状数组（BIT）](#6-树状数组bit)
7. [前缀和与差分（2维）](#7-前缀和与差分2维)
8. [位运算](#8-位运算)
9. [双指针](#9-双指针)
10. [栈](#10-栈)
11. [优先队列（堆）](#11-优先队列堆)
12. [map](#12-map)
13. [set](#13-set)
14. [bitset](#14-bitset)
15. [单调栈](#15-单调栈)
16. [单调队列](#16-单调队列)
17. [并查集](#17-并查集)
18. [最短路](#18-最短路)
19. [快速幂](#19-快速幂)
20. [GCD 与 LCM](#20-gcd-与-lcm)
21. [组合数](#21-组合数)
22. [线性 DP](#22-线性-dp)
23. [区间 DP](#23-区间-dp)
24. [树形 DP](#24-树形-dp)
25. [01 背包](#25-01-背包)
26. [完全背包](#26-完全背包)
27. [质因数分解](#27-质因数分解)
28. [最小生成树](#28-最小生成树)
29. [图的存储](#29-图的存储)

---

## 1. 二分查找

**思路**：在有序数组中，每次取中间值与目标比较，缩小范围，时间复杂度 O(log n)。

```cpp
// 查找第一个 >= target 的位置（lower_bound）
int binary_search(int a[], int n, int target) {
    int l = 0, r = n - 1;
    while (l < r) {
        int mid = l + (r - l) / 2;
        if (a[mid] < target) l = mid + 1;
        else r = mid;
    }
    return l; // a[l] 即为第一个 >= target 的元素
}

// 查找最后一个 <= target 的位置（upper_bound - 1）
int binary_search2(int a[], int n, int target) {
    int l = 0, r = n - 1;
    while (l < r) {
        int mid = l + (r - l + 1) / 2; // 上取整，防止死循环
        if (a[mid] > target) r = mid - 1;
        else l = mid;
    }
    return l;
}
```

> **注意**：`mid = l + (r - l) / 2` 防溢出；当 `l = mid` 时，`mid` 要上取整，否则死循环。

---

## 2. 二分答案

**思路**：答案具有单调性（越大越满足 / 越小越满足），对答案进行二分，用 `check(mid)` 判断可行性。

```cpp
// 示例：找满足条件的最小答案
bool check(int mid) {
    // 判断 mid 是否满足题目要求
    // ...
    return true; // 满足返回 true
}

int solve() {
    int l = 下界, r = 上界;
    while (l < r) {
        int mid = l + (r - l) / 2;
        if (check(mid)) r = mid;    // mid 满足，尝试更小
        else l = mid + 1;           // mid 不满足，增大
    }
    return l; // 最小满足条件的答案
}
```

---

## 3. DFS（深度优先搜索）

**思路**：递归地向深处探索，回溯时恢复现场，适合求全部方案、连通性等问题。

```cpp
int n, m;
bool vis[105][105]; // 访问标记
int dx[] = {0, 0, 1, -1};
int dy[] = {1, -1, 0, 0};

void dfs(int x, int y) {
    vis[x][y] = true;
    // 处理当前节点 ...
    for (int d = 0; d < 4; d++) {
        int nx = x + dx[d], ny = y + dy[d];
        if (nx >= 1 && nx <= n && ny >= 1 && ny <= m && !vis[nx][ny]) {
            dfs(nx, ny);
        }
    }
}

// 排列/组合型 DFS（回溯）
int path[105], cnt;
bool used[105];

void dfs2(int step, int total) {
    if (step == total + 1) {
        // 记录答案
        cnt++;
        return;
    }
    for (int i = 1; i <= total; i++) {
        if (!used[i]) {
            used[i] = true;
            path[step] = i;
            dfs2(step + 1, total);
            used[i] = false; // 回溯
        }
    }
}
```

---

## 4. BFS（广度优先搜索）

**思路**：用队列层层扩展，适合求最短步数/最短路（边权为 1 时）。

```cpp
#include <queue>
using namespace std;

int n, m;
int dist[105][105];
bool vis[105][105];
int dx[] = {0, 0, 1, -1};
int dy[] = {1, -1, 0, 0};

void bfs(int sx, int sy) {
    queue<pair<int,int>> q;
    q.push({sx, sy});
    vis[sx][sy] = true;
    dist[sx][sy] = 0;
    while (!q.empty()) {
        auto [x, y] = q.front(); q.pop();
        for (int d = 0; d < 4; d++) {
            int nx = x + dx[d], ny = y + dy[d];
            if (nx >= 1 && nx <= n && ny >= 1 && ny <= m && !vis[nx][ny]) {
                vis[nx][ny] = true;
                dist[nx][ny] = dist[x][y] + 1;
                q.push({nx, ny});
            }
        }
    }
}
```

---

## 5. 离散化

**思路**：将大范围稀疏的数据映射到小范围连续整数，常配合树状数组/线段树使用。

```cpp
#include <vector>
#include <algorithm>
using namespace std;

vector<int> nums; // 需要离散化的数

void discretize() {
    sort(nums.begin(), nums.end());
    nums.erase(unique(nums.begin(), nums.end()), nums.end());
}

// 查询 x 离散化后的编号（1-indexed）
int get_id(int x) {
    return lower_bound(nums.begin(), nums.end(), x) - nums.begin() + 1;
}

// 使用示例
// 先将所有用到的值加入 nums，调用 discretize()
// 之后用 get_id(x) 获取编号
```

---

## 6. 树状数组（BIT）

**思路**：高效支持单点更新与前缀查询，时间复杂度均为 O(log n)。`lowbit(x) = x & (-x)`。

```cpp
const int MAXN = 1e5 + 5;
int c[MAXN], n;

int lowbit(int x) { return x & (-x); }

// 单点加：a[i] += val
void update(int i, int val) {
    for (; i <= n; i += lowbit(i))
        c[i] += val;
}

// 前缀和：sum(1..i)
int query(int i) {
    int sum = 0;
    for (; i > 0; i -= lowbit(i))
        sum += c[i];
    return sum;
}

// 区间和：sum(l..r)
int range_query(int l, int r) {
    return query(r) - query(l - 1);
}
```

---

## 7. 前缀和与差分（2维）

### 一维前缀和

```cpp
int a[MAXN], pre[MAXN];
// 建立
for (int i = 1; i <= n; i++) pre[i] = pre[i-1] + a[i];
// 区间查询 [l, r]
int sum = pre[r] - pre[l-1];
```

### 一维差分

```cpp
int diff[MAXN]; // diff[i] = a[i] - a[i-1]
// 区间 [l, r] 加 val
diff[l] += val;
diff[r+1] -= val;
// 还原数组
for (int i = 1; i <= n; i++) a[i] = a[i-1] + diff[i];
```

### 二维前缀和

```cpp
int a[MAXN][MAXN], pre[MAXN][MAXN];
// 建立
for (int i = 1; i <= n; i++)
    for (int j = 1; j <= m; j++)
        pre[i][j] = a[i][j] + pre[i-1][j] + pre[i][j-1] - pre[i-1][j-1];
// 子矩阵 (r1,c1)~(r2,c2) 的和
int sum = pre[r2][c2] - pre[r1-1][c2] - pre[r2][c1-1] + pre[r1-1][c1-1];
```

### 二维差分

```cpp
int diff[MAXN][MAXN];
// 子矩阵 (r1,c1)~(r2,c2) 全部加 val
void add(int r1, int c1, int r2, int c2, int val) {
    diff[r1][c1]     += val;
    diff[r1][c2+1]   -= val;
    diff[r2+1][c1]   -= val;
    diff[r2+1][c2+1] += val;
}
// 还原二维数组
for (int i = 1; i <= n; i++)
    for (int j = 1; j <= m; j++)
        a[i][j] = a[i-1][j] + a[i][j-1] - a[i-1][j-1] + diff[i][j];
```

---

## 8. 位运算

```cpp
// 常用操作
x & 1           // 判断奇偶（1 为奇，0 为偶）
x >> 1          // 除以 2
x << 1          // 乘以 2
x & (x - 1)     // 清除最低位的 1（判断是否为 2 的幂：x & (x-1) == 0）
x & (-x)        // 取最低位的 1（lowbit）
__builtin_popcount(x) // 统计 x 二进制中 1 的个数

// 枚举子集（状压 DP 常用）
for (int s = mask; s > 0; s = (s - 1) & mask) {
    // 处理子集 s
}

// 第 k 位（0-indexed）是否为 1
bool bit_k = (x >> k) & 1;

// 将第 k 位置 1
x |= (1 << k);

// 将第 k 位清 0
x &= ~(1 << k);

// 翻转第 k 位
x ^= (1 << k);
```

---

## 9. 双指针

**思路**：用两个指针（同向或对向）在数组上移动，避免嵌套循环，时间复杂度 O(n)。

```cpp
// 对向双指针：两数之和为 target（有序数组）
void two_sum(int a[], int n, int target) {
    int l = 0, r = n - 1;
    while (l < r) {
        int s = a[l] + a[r];
        if (s == target) { /* 找到 */ l++; r--; }
        else if (s < target) l++;
        else r--;
    }
}

// 同向双指针：最长无重复字符子串
#include <unordered_map>
int longest_unique(string s) {
    unordered_map<char, int> cnt;
    int ans = 0;
    for (int l = 0, r = 0; r < (int)s.size(); r++) {
        cnt[s[r]]++;
        while (cnt[s[r]] > 1) { // 有重复，收缩左端
            cnt[s[l]]--;
            l++;
        }
        ans = max(ans, r - l + 1);
    }
    return ans;
}
```

---

## 10. 栈

**思路**：后进先出（LIFO）。用于括号匹配、表达式求值、DFS 迭代版等。

```cpp
#include <stack>
using namespace std;

stack<int> st;
st.push(x);     // 入栈
st.top();       // 查看栈顶
st.pop();       // 出栈
st.empty();     // 判断空
st.size();      // 栈的大小

// 括号匹配示例
bool is_valid(string s) {
    stack<char> st;
    for (char c : s) {
        if (c == '(' || c == '[' || c == '{') st.push(c);
        else {
            if (st.empty()) return false;
            char top = st.top(); st.pop();
            if (c == ')' && top != '(') return false;
            if (c == ']' && top != '[') return false;
            if (c == '}' && top != '{') return false;
        }
    }
    return st.empty();
}
```

---

## 11. 优先队列（堆）

**思路**：STL `priority_queue` 默认大根堆（最大值先出）。

```cpp
#include <queue>
using namespace std;

// 大根堆（默认）
priority_queue<int> pq;

// 小根堆
priority_queue<int, vector<int>, greater<int>> min_pq;

pq.push(x);   // 插入
pq.top();     // 查看堆顶
pq.pop();     // 删除堆顶
pq.empty();   // 判断空

// 自定义排序（按结构体第一个字段升序）
struct Node { int dist, id; };
auto cmp = [](Node a, Node b) { return a.dist > b.dist; }; // 小根堆
priority_queue<Node, vector<Node>, decltype(cmp)> pq2(cmp);
```

---

## 12. map

**思路**：有序键值对，底层红黑树，O(log n) 增删查。`unordered_map` 哈希，均摊 O(1)。

```cpp
#include <map>
#include <unordered_map>
using namespace std;

map<string, int> mp;
mp["apple"] = 5;          // 插入/修改
mp.count("apple");        // 是否存在（0 或 1）
mp.find("apple");         // 返回迭代器，mp.end() 表示不存在
mp.erase("apple");        // 删除
mp.size();                // 大小

// 遍历
for (auto& [key, val] : mp) {
    // key, val
}

// unordered_map 用法相同，但无序，速度更快
unordered_map<int, int> ump;
```

---

## 13. set

**思路**：有序不重复集合，O(log n) 增删查。`unordered_set` 哈希，均摊 O(1)。

```cpp
#include <set>
using namespace std;

set<int> s;
s.insert(x);          // 插入
s.erase(x);           // 删除
s.count(x);           // 是否存在（0 或 1）
s.find(x);            // 返回迭代器
s.lower_bound(x);     // 第一个 >= x 的迭代器
s.upper_bound(x);     // 第一个 > x 的迭代器
s.size();             // 大小

// multiset 允许重复元素
multiset<int> ms;
ms.insert(x);
ms.erase(ms.find(x)); // 只删除一个
```

---

## 14. bitset

**思路**：固定长度的二进制位集合，支持位运算，空间压缩，常用于状压/集合运算。

```cpp
#include <bitset>
using namespace std;

const int N = 1000;
bitset<N> bs;

bs.set(i);         // 第 i 位置 1
bs.reset(i);       // 第 i 位置 0
bs.flip(i);        // 第 i 位翻转
bs.test(i);        // 第 i 位是否为 1
bs.count();        // 统计 1 的个数
bs.any();          // 是否有 1
bs.none();         // 是否全 0

// 位运算（整体操作，速度极快）
bitset<N> a, b;
a & b;  a | b;  a ^ b;  ~a;
a <<= k;  a >>= k;

// 示例：01背包用 bitset 优化（可达性）
bitset<MAXW+1> dp;
dp[0] = 1;
for (int i = 0; i < n; i++)
    dp |= (dp << w[i]); // dp 左移 w[i] 位后与自身取或
```

---

## 15. 单调栈

**思路**：栈内元素保持单调（递增或递减），用于求每个元素左/右第一个更大/更小的元素，O(n)。

```cpp
// 求每个元素右边第一个更大的元素（Next Greater Element）
int a[MAXN], nge[MAXN]; // nge[i] = a[i] 右边第一个比它大的值的下标
stack<int> st; // 存下标

for (int i = n - 1; i >= 0; i--) {
    while (!st.empty() && a[st.top()] <= a[i]) st.pop();
    nge[i] = st.empty() ? -1 : st.top(); // -1 表示不存在
    st.push(i);
}

// 求每个元素左边第一个更小的元素（Previous Less Element）
int ple[MAXN];
while (!st.empty()) st.pop();
for (int i = 0; i < n; i++) {
    while (!st.empty() && a[st.top()] >= a[i]) st.pop();
    ple[i] = st.empty() ? -1 : st.top();
    st.push(i);
}
```

---

## 16. 单调队列

**思路**：双端队列（deque）维护窗口内的最大/最小值，用于滑动窗口问题，O(n)。

```cpp
#include <deque>
using namespace std;

// 滑动窗口最大值（窗口大小为 k）
void sliding_window_max(int a[], int n, int k) {
    deque<int> dq; // 存下标，队首是窗口内最大值的下标
    for (int i = 0; i < n; i++) {
        // 移出窗口外的元素
        while (!dq.empty() && dq.front() < i - k + 1) dq.pop_front();
        // 维护单调递减（从队尾移出更小的元素）
        while (!dq.empty() && a[dq.back()] <= a[i]) dq.pop_back();
        dq.push_back(i);
        if (i >= k - 1) printf("%d ", a[dq.front()]); // 窗口最大值
    }
}
```

---

## 17. 并查集

**思路**：维护元素的集合，支持合并（union）和查询同一集合（find），路径压缩 + 按秩合并后近似 O(1)。

```cpp
const int MAXN = 1e5 + 5;
int parent[MAXN], rnk[MAXN];

void init(int n) {
    for (int i = 1; i <= n; i++) parent[i] = i, rnk[i] = 0;
}

// 路径压缩
int find(int x) {
    if (parent[x] != x) parent[x] = find(parent[x]);
    return parent[x];
}

// 按秩合并
void unite(int x, int y) {
    int px = find(x), py = find(y);
    if (px == py) return;
    if (rnk[px] < rnk[py]) swap(px, py);
    parent[py] = px;
    if (rnk[px] == rnk[py]) rnk[px]++;
}

bool same(int x, int y) { return find(x) == find(y); }
```

---

## 18. 最短路

### Dijkstra（单源，非负边权）O((V+E) log V)

```cpp
#include <vector>
#include <queue>
#include <climits>
using namespace std;

const int MAXN = 1e5 + 5;
typedef pair<int,int> pii; // {dist, node}
vector<pii> adj[MAXN]; // adj[u] = {{w, v}, ...}
int dist[MAXN];

void dijkstra(int s, int n) {
    fill(dist, dist + n + 1, INT_MAX);
    dist[s] = 0;
    priority_queue<pii, vector<pii>, greater<pii>> pq;
    pq.push({0, s});
    while (!pq.empty()) {
        auto [d, u] = pq.top(); pq.pop();
        if (d > dist[u]) continue; // 已有更短路，跳过
        for (auto [w, v] : adj[u]) {
            if (dist[u] + w < dist[v]) {
                dist[v] = dist[u] + w;
                pq.push({dist[v], v});
            }
        }
    }
}
```

### Bellman-Ford（单源，可有负边权，检测负环）O(VE)

```cpp
struct Edge { int u, v, w; };
Edge edges[MAXM];
int dist[MAXN];

bool bellman_ford(int s, int n, int m) {
    fill(dist, dist + n + 1, INT_MAX);
    dist[s] = 0;
    for (int i = 0; i < n - 1; i++) {
        for (int j = 0; j < m; j++) {
            int u = edges[j].u, v = edges[j].v, w = edges[j].w;
            if (dist[u] != INT_MAX && dist[u] + w < dist[v])
                dist[v] = dist[u] + w;
        }
    }
    // 检测负环
    for (int j = 0; j < m; j++) {
        int u = edges[j].u, v = edges[j].v, w = edges[j].w;
        if (dist[u] != INT_MAX && dist[u] + w < dist[v]) return false; // 有负环
    }
    return true;
}
```

### Floyd（多源，可有负边，O(V³)）

```cpp
const int INF = 1e9;
int d[305][305]; // d[i][j] = i 到 j 的最短距离

void floyd(int n) {
    // 初始化：d[i][i]=0，其余 d[i][j] 为边权或 INF
    for (int k = 1; k <= n; k++)
        for (int i = 1; i <= n; i++)
            for (int j = 1; j <= n; j++)
                if (d[i][k] + d[k][j] < d[i][j])
                    d[i][j] = d[i][k] + d[k][j];
}
```

---

## 19. 快速幂

**思路**：利用 `a^n = a^(n/2) * a^(n/2)`，递归/迭代实现，O(log n)。

```cpp
// 迭代快速幂
long long fast_pow(long long base, long long exp, long long mod) {
    long long result = 1;
    base %= mod;
    while (exp > 0) {
        if (exp & 1) result = result * base % mod;
        base = base * base % mod;
        exp >>= 1;
    }
    return result;
}

// 不取模版本
long long fast_pow(long long base, long long exp) {
    long long result = 1;
    while (exp > 0) {
        if (exp & 1) result *= base;
        base *= base;
        exp >>= 1;
    }
    return result;
}
```

---

## 20. GCD 与 LCM

```cpp
// 辗转相除法（欧几里得算法）
long long gcd(long long a, long long b) {
    return b == 0 ? a : gcd(b, a % b);
}

// 最小公倍数
long long lcm(long long a, long long b) {
    return a / gcd(a, b) * b; // 先除后乘，防溢出
}

// C++17 可直接使用
#include <numeric>
// __gcd(a, b)  或  std::gcd(a, b)（C++17）
// std::lcm(a, b)（C++17）
```

---

## 21. 组合数

### 预处理阶乘（适合多次查询，O(n) 预处理，O(1) 查询）

```cpp
const int MAXN = 1e6 + 5;
const int MOD = 1e9 + 7;
long long fac[MAXN], inv_fac[MAXN];

long long fast_pow(long long a, long long b, long long mod) {
    long long res = 1; a %= mod;
    for (; b > 0; b >>= 1) {
        if (b & 1) res = res * a % mod;
        a = a * a % mod;
    }
    return res;
}

void precompute(int n) {
    fac[0] = 1;
    for (int i = 1; i <= n; i++) fac[i] = fac[i-1] * i % MOD;
    inv_fac[n] = fast_pow(fac[n], MOD - 2, MOD);
    for (int i = n - 1; i >= 0; i--) inv_fac[i] = inv_fac[i+1] * (i+1) % MOD;
}

long long C(int n, int k) {
    if (k < 0 || k > n) return 0;
    return fac[n] % MOD * inv_fac[k] % MOD * inv_fac[n-k] % MOD;
}
```

### 杨辉三角（适合 n, k 较小，O(n²) 预处理）

```cpp
long long C[105][105]; // C[i][j] = C(i, j)
void pascal(int n) {
    for (int i = 0; i <= n; i++) {
        C[i][0] = 1;
        for (int j = 1; j <= i; j++)
            C[i][j] = (C[i-1][j-1] + C[i-1][j]) % MOD;
    }
}
```

---

## 22. 线性 DP

### 最长递增子序列（LIS）O(n log n)

```cpp
#include <vector>
#include <algorithm>
using namespace std;

int lis(vector<int>& a) {
    vector<int> dp; // dp[i] = 长度为 i+1 的 LIS 的最小结尾元素
    for (int x : a) {
        auto it = lower_bound(dp.begin(), dp.end(), x);
        if (it == dp.end()) dp.push_back(x);
        else *it = x;
    }
    return dp.size();
}
```

### 最长公共子序列（LCS）O(nm)

```cpp
int lcs(string a, string b) {
    int n = a.size(), m = b.size();
    vector<vector<int>> dp(n+1, vector<int>(m+1, 0));
    for (int i = 1; i <= n; i++)
        for (int j = 1; j <= m; j++) {
            if (a[i-1] == b[j-1]) dp[i][j] = dp[i-1][j-1] + 1;
            else dp[i][j] = max(dp[i-1][j], dp[i][j-1]);
        }
    return dp[n][m];
}
```

---

## 23. 区间 DP

**思路**：`dp[l][r]` 表示区间 `[l, r]` 的最优值，枚举分割点 k，从小区间推大区间。

```cpp
// 矩阵链乘 / 石子合并等区间 DP 框架
int dp[305][305], cost[305][305];

void interval_dp(int n) {
    // 初始化 dp[i][i]
    for (int i = 1; i <= n; i++) dp[i][i] = 0;

    // len 为区间长度，从 2 开始枚举
    for (int len = 2; len <= n; len++) {
        for (int l = 1; l + len - 1 <= n; l++) {
            int r = l + len - 1;
            dp[l][r] = INT_MAX; // 求最小
            for (int k = l; k < r; k++) {
                dp[l][r] = min(dp[l][r], dp[l][k] + dp[k+1][r] + cost[l][r]);
            }
        }
    }
}
```

---

## 24. 树形 DP

**思路**：在树上做 DP，通常以 DFS 后序遍历，先处理子节点再更新父节点。

```cpp
#include <vector>
using namespace std;

const int MAXN = 1e5 + 5;
vector<int> adj[MAXN]; // 邻接表（无根树）
int dp[MAXN][2]; // dp[u][0/1]：以 u 为根，不选/选 u 的最大独立集

void dfs(int u, int parent) {
    dp[u][0] = 0; // 不选 u
    dp[u][1] = 1; // 选 u
    for (int v : adj[u]) {
        if (v == parent) continue;
        dfs(v, u);
        dp[u][0] += max(dp[v][0], dp[v][1]); // 不选 u，子节点任意
        dp[u][1] += dp[v][0];                 // 选 u，子节点不能选
    }
}

// 调用：dfs(root, -1);  答案：max(dp[root][0], dp[root][1])
```

---

## 25. 01 背包

**思路**：每件物品只能选一次。`dp[j]` = 容量为 j 时的最大价值。**倒序**枚举容量防止重复选。

```cpp
int n, W;              // 物品数量，背包容量
int w[MAXN], v[MAXN]; // 重量，价值
int dp[MAXW];          // dp[j] = 容量 j 时最大价值

void knapsack_01() {
    fill(dp, dp + W + 1, 0);
    for (int i = 1; i <= n; i++)
        for (int j = W; j >= w[i]; j--) // 倒序！
            dp[j] = max(dp[j], dp[j - w[i]] + v[i]);
    // 答案：dp[W]
}
```

---

## 26. 完全背包

**思路**：每件物品可选无限次。与 01 背包区别：**正序**枚举容量（允许重复选）。

```cpp
void knapsack_complete() {
    fill(dp, dp + W + 1, 0);
    for (int i = 1; i <= n; i++)
        for (int j = w[i]; j <= W; j++) // 正序！
            dp[j] = max(dp[j], dp[j - w[i]] + v[i]);
    // 答案：dp[W]
}
```

---

## 27. 质因数分解

**思路**：从 2 开始试除，O(√n)。

```cpp
#include <map>
using namespace std;

map<int, int> factorize(int n) {
    map<int, int> factors; // 质因数 -> 指数
    for (int i = 2; (long long)i * i <= n; i++) {
        while (n % i == 0) {
            factors[i]++;
            n /= i;
        }
    }
    if (n > 1) factors[n]++; // n 本身是质数
    return factors;
}

// 使用示例：
// auto f = factorize(360); // {2:3, 3:2, 5:1} 即 2^3 * 3^2 * 5
```

---

## 28. 最小生成树

### Kruskal（基于边，适合稀疏图）O(E log E)

```cpp
#include <algorithm>
using namespace std;

struct Edge { int u, v, w; };
int parent[MAXN];
int find(int x) { return parent[x] == x ? x : parent[x] = find(parent[x]); }

int kruskal(vector<Edge>& edges, int n) {
    sort(edges.begin(), edges.end(), [](Edge& a, Edge& b){ return a.w < b.w; });
    for (int i = 1; i <= n; i++) parent[i] = i;
    int total = 0, cnt = 0;
    for (auto& e : edges) {
        int pu = find(e.u), pv = find(e.v);
        if (pu != pv) {
            parent[pu] = pv;
            total += e.w;
            cnt++;
            if (cnt == n - 1) break; // 已有 n-1 条边
        }
    }
    return total; // 最小生成树总权值（若 cnt < n-1 则图不连通）
}
```

### Prim（基于点，适合稠密图）O(V²) 或用堆 O(E log V)

```cpp
// 适合用邻接矩阵的稠密图，O(V²)
const int INF = 1e9;
int g[305][305]; // 邻接矩阵
int key[305];    // key[i] = 当前点 i 到已选集合的最短边权
bool inMST[305];

int prim(int n) {
    fill(key, key + n + 1, INF);
    fill(inMST, inMST + n + 1, false);
    key[1] = 0;
    int total = 0;
    for (int i = 0; i < n; i++) {
        // 找 key 最小且不在 MST 中的点
        int u = -1;
        for (int v = 1; v <= n; v++)
            if (!inMST[v] && (u == -1 || key[v] < key[u])) u = v;
        inMST[u] = true;
        total += key[u];
        for (int v = 1; v <= n; v++)
            if (!inMST[v] && g[u][v] < key[v])
                key[v] = g[u][v];
    }
    return total;
}
```

---

## 29. 图的存储

### 邻接矩阵（适合稠密图，V ≤ 1000）

```cpp
int g[1005][1005]; // g[u][v] = 边权，0 或 INF 表示无边
// 加边：g[u][v] = w; （无向图还需 g[v][u] = w;）
```

### 邻接表（适合稀疏图，推荐）

```cpp
#include <vector>
using namespace std;
vector<pair<int,int>> adj[MAXN]; // adj[u] = {{v, w}, ...}

// 加有向边 u->v，权重 w
void add_edge(int u, int v, int w) {
    adj[u].push_back({v, w});
}
// 无向图再加：adj[v].push_back({u, w});
```

### 链式前向星（静态数组实现邻接表，效率高）

```cpp
const int MAXN = 1e5 + 5;
const int MAXM = 2e5 + 5;
int head[MAXN], nxt[MAXM], to[MAXM], w[MAXM], tot = 0;

void init(int n) { fill(head, head + n + 1, -1); tot = 0; }

void add_edge(int u, int v, int ww) {
    to[tot] = v;
    w[tot] = ww;
    nxt[tot] = head[u];
    head[u] = tot++;
}

// 遍历 u 的所有出边
// for (int e = head[u]; e != -1; e = nxt[e]) { int v = to[e], ww = w[e]; }
```

---

> 以上模板均基于 C++，适用于竞赛常见场景，请根据实际题目灵活调整边界和初始值。
